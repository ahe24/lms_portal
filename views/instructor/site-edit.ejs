<%- include('../partials/head') %>
    <%- include('../partials/sidebar') %>

        <div class="page-header">
            <h1>교과 사이트 수정</h1>
            <p>등록된 외부 교육 사이트 정보를 수정합니다.</p>
        </div>

        <div class="card" style="max-width: 600px;">
            <form method="POST" action="/instructor/sites/<%= site.id %>/edit">
                <div class="form-group">
                    <label class="form-label">식별 문자열 (Slug) *</label>
                    <input type="text" name="slug" class="form-input" value="<%= site.slug %>" required
                        pattern="[a-zA-Z0-9-_]+" title="영문, 숫자, 하이픈(-), 언더바(_)만 가능합니다">
                    <small class="text-muted text-sm">URL 경로로 사용됩니다 (예: linux-lect)</small>
                </div>
                <div class="form-group">
                    <label class="form-label">사이트명 *</label>
                    <input type="text" name="name" class="form-input" value="<%= site.name %>" required
                        placeholder="예: FPGA 리눅스 커널 분석">
                </div>
                <div class="form-group">
                    <label class="form-label">접속 URL *</label>
                    <input type="url" name="url" class="form-input" value="<%= site.url %>" required
                        placeholder="https://example.com">
                </div>
                <div class="form-group">
                    <label class="form-label">설명</label>
                    <textarea name="description" class="form-input"
                        placeholder="사이트에 대한 간략한 설명"><%= site.description %></textarea>
                </div>
                <div class="flex gap-2">
                    <button type="submit" class="btn btn-primary">저장</button>
                    <a href="/instructor/sites" class="btn btn-outline">취소</a>
                </div>
            </form>
        </div>

        <%- include('../partials/footer') %>